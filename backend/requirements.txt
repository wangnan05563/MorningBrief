# ==== Web 框架 ====
fastapi==0.110.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9              # 文件上传（广告素材）
slowapi==0.1.9                       # 限流中间件（V1.2 替代 Nginx limit_req）

# ==== 数据库（V1.2：SQLite 替代 MySQL） ====
sqlalchemy[asyncio]==2.0.25
aiosqlite==0.19.0                    # SQLite async 驱动（替代 aiomysql）
alembic==1.13.1                      # 数据库迁移

# ==== 缓存（V1.2：TTLCache 替代 Redis） ====
cachetools==5.3.3                    # TTLCache 进程内缓存（替代 redis-py）

# ==== 鉴权 ====
pyjwt==2.8.0                         # JWT 签发与验签
bcrypt==4.1.2                        # 密码哈希

# ==== LLM / TTS ====
openai==1.12.0                       # 通义千问兼容 OpenAI SDK
tenacity==8.2.3                      # 重试装饰器
edge-tts==7.2.8                      # 微软 Edge-TTS 免费方案；6.x 的 TrustedClientToken 已被微软封禁（403），必须 >=7.2.8

# ==== 爬虫（V1.2：httpx + selectolax 替代 Scrapy + Playwright） ====
httpx==0.27.0                        # 异步 HTTP 客户端（替代 Scrapy，统一外部 API 调用驱动）
feedparser==6.0.11                   # RSS 解析
selectolax==0.4.10                  # HTML 解析（替代 newspaper3k + Scrapy 选择器）；0.3.21 无 cp314 wheel 需源码编译失败，升级到 0.4.10 有预编译 wheel

# ==== 算法 ====
jieba==0.42.1                        # 中文分词（SimHash 用）
pyahocorasick==2.3.1                 # AC 自动机（敏感词匹配，import 名为 ahocorasick）

# ==== 对象存储 ====
cos-python-sdk-v5==1.9.30           # 腾讯云 COS（音频存储 + C 端 API 共享层）

# ==== 配置与工具 ====
pydantic-settings==2.1.0            # 环境变量配置
pyyaml==6.0.1                        # YAML 配置解析
python-dotenv==1.0.1

# ==== 调度 ====
apscheduler==3.10.4

# ==== 日志 ====
loguru==0.7.2
colorama==0.4.6                        # Windows cmd 终端 ANSI 颜色兼容（loguru 底层依赖）

# ==== 测试依赖 ====
# pytest 8.0.0 + pytest-asyncio 0.23.0 在 Python 3.12 收集阶段报
# 'Package' object has no attribute 'obj'，升级到 0.24 修复兼容性
pytest==8.4.2
pytest-asyncio==0.24.0
