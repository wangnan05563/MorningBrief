# MorningBrief 代码审查报告

## 基本信息

- **审查版本**：v1.0.0
- **审查模式**：[全量审查 / 增量审查 / 指定文件审查 / 片段评审]
- **审查范围**：{{review_scope}}
- **审查文件数**：X 个
- **审查时间**：YYYY-MM-DD HH:MM:SS
- **审查人**：{{skill_name}} Skill
- **上次审查**：[有/无]

## 相对于上次审查的变化

| 状态 | 数量 | 说明 |
|------|------|------|
| 🆕 新增 | X | 本次新发现的问题 |
| ✅ 已修复 | X | 上次存在的问题已修复 |
| ⚠️ 仍存在 | X | 上次存在的问题仍未修复 |

## 审查结果摘要

- 🔴 阻塞问题：X 个（必须修复）
- 🟠 严重问题：X 个（强烈建议修复）
- 🟡 警告问题：X 个（建议修复）
- 🟢 优化建议：X 个（可选）

### 按 severity 分布

| Severity | 数量 |
|----------|------|
| CRITICAL | X |
| HIGH | X |
| MEDIUM | X |
| LOW | X |
| INFO | X |

### 按 category 分布

{{category_distribution_table}}

## 硬约束合规性检查结果

| 硬约束规则 | 状态 | 违规位置 |
|------------|------|----------|
{{hard_constraint_rows}}

**合并结论**：[允许合并 / 阻止合并（存在 CRITICAL 违规）]

{{skill_specific_section}}

## 详细问题列表

### 🔴 阻塞问题（必须修复）

1. **问题描述**：[具体问题描述]
   - **位置**：{{typical_file_location}}
   - **当前代码**：
     ```{{code_block_lang}}
     // 问题代码示例
     ```
   - **修复建议**：
     ```{{code_block_lang}}
     // 修复后的代码示例
     ```
   - **参考规范**：[对应 SKILL.md 维度章节]

### 🟠 严重问题（强烈建议修复）

1. **问题描述**：[具体问题描述]
   - **位置**：{{typical_file_location}}
   - **修复建议**：[具体建议]

### 🟡 警告问题（建议修复）

1. **问题描述**：[具体问题描述]
   - **位置**：{{typical_file_location}}
   - **修复建议**：[具体建议]

### 🟢 优化建议（可选）

1. **建议描述**：[具体建议]
   - **位置**：{{typical_file_location}}
   - **优化方案**：[具体方案]

## ✅ 好的实践

- [正面反馈：列出本次审查中发现的好实践]
{{good_practices_placeholder}}

## 测试运行结果

- **测试命令**：{{test_command}}
- **测试结果**：[通过 / 失败]
{{test_extra_info}}

## 审查结论

- [ ] 通过（无阻塞问题）
- [ ] 有条件通过（仅警告和提示级别问题）
- [ ] 不通过（存在阻塞或严重问题）

## 修复验证

修复完成后，请重新运行审查确认问题已解决：

```powershell
# 1. 重新运行快速自检
pwsh {{auto_scan_path}}

# 2. 重新运行人工评审
# 调用 {{skill_name}} 技能
```

## 报告归档

报告保存路径：`{{report_archive_path}}`

---

## 四维度复盘

> 基于本次 MorningBrief 代码审查完整过程的复盘，沉淀可复用的工作流模板与判断逻辑。

### 维度 1：成功执行任务的完整步骤

- 本次审查在 XX 类目下识别出 `N` 个问题，其中 `M` 个被成功闭环
- 关键成功路径（按时间顺序）：
  1. `step 1`: 范围识别
  2. `step 2`: 配置加载
  3. `step 3`: 快速自检
  4. `step 4`: 维度遍历
  5. `step 5`: 报告生成
  6. `step 6`: 四维度复盘
  {{workflow_extra_steps}}

### 维度 2：任务执行过程中的不确定性与失败点

| 失败点 | 触发条件 | 影响范围 | 根因 | 修复方式 |
|--------|----------|----------|------|----------|
{{failure_points_placeholder}}

### 维度 3：可抽象的固定流程与判断逻辑

| 模板 | 核心判断信号 | 落地配置节点 |
|------|--------------|--------------|
{{abstraction_templates_placeholder}}

### 维度 4：适用场景与不适用场景

| 模板 | 适用场景 | 不适用场景 |
|------|----------|------------|
{{applicability_table_placeholder}}

## 配置变更点

> 本次审查触发的 `config.yaml` 节点变更建议。所有变更遵循"无硬编码"原则，仅调整阈值/白名单/关键字等参数化配置，不引入新的硬编码业务值。

| 变更类型 | 配置节点 | 当前值 | 建议值 | 变更理由 | 影响范围 |
|----------|----------|--------|--------|----------|----------|
{{config_changes_placeholder}}

**变更后自检清单**：
- [ ] 无硬编码新增（所有数值/列表/关键字均在 config 节点管理）
- [ ] 通用性未降低（参数化配置可被不同业务场景覆盖）
- [ ] 现有违规检测不失效（回归测试通过）
- [ ] SKILL.md 维度与本变更一致

## SonarQube 规则映射

| SQ 规则 | 审查维度 | 编码规范 | 元规范 | 严重级别 | 修复建议 |
|---------|---------|---------|--------|---------|----------|
{{sonarqube_mapping_rows}}

## 修复优先级矩阵

| 问题严重级别 | 处理策略 | 阻塞发版 |
|------------|---------|----------|
| 阻塞 (blocker) | block_release | ✓ |
| 严重 (critical) | block_release | ✓ |
| 主要 (major) | fix_before_release | ✗（但发版前必须修复） |
| 警告 (warning) | fix_next_iteration | ✗ |
| 建议 (suggestion) | log_only | ✗ |
| 信息 (info) | log_only | ✗ |
